// CALLUM KOH IS HERE TO SAVE THE DAY! // SO IS THE PUZZLEQUEST TEAMS #include <math.h>#include "mandelbrot.h"#define MAX_ITERATION 256 // Here's my "escapeSteps" function. It passes all the test cases on mandelbrotTest.c!int escapeSteps(double a, double b) {  int steps = 1;  double c = a;  double d = b;  double f;  double e;    while (steps < MAX_ITERATION) {    if (sqrt((a*a + b*b)) > 2) {      break;    } else {      steps++;      e = (a * a) - (b * b) + c;      f = (2.0 * a * b) + d;      a = e;      b = f;    }  }  return steps;}